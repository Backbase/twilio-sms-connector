twilio:
  accountSid: dummySid
  authToken: dummyToken
  fromNumber: dummyNumber

# Configure Internal JWT handler
sso:
  jwt:
    internal:
      signature:
        key:
          type: ENV
          value: SIG_SECRET_KEY

backbase:
  sms:
      worker-count: 2